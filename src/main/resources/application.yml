spring:
  datasource:
    url: jdbc:mysql://localhost:3306/concert_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    openai:
      api-key: sk-1e3f3c634882483e8f53a3610496b72d   # 请使用你自己的 DeepSeek key
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.8

  data:
    redis:
      enabled: true
      port: 6379
      database: 1
      host: localhost

  task:
    scheduling:
      enabled: true
      pool:
        size: 2

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

server:
  port: 8081
